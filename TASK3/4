#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    int x;
    printf("Enter the string:");
    gets(str);
    for(int i=0;str[i]!='\0';i++)
    {
        x ++;
    }
    printf("The length of the string is %d",x);
}
