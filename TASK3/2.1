// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>

int main() {
char str[10],c;
int i=0,count=0;
printf("Enter the string:");
gets(str);
printf("Enter the letter to be searched:");
c = getchar();
while(str[i] != '\0')
{
    if(str[i] == c)
    {
        count++;
    }
    i++;
}
printf("the letter is reapted %d times",count);
}
