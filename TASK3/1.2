// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>

int main() {
char str[] = "Engineering";
int length = strlen(str);
char x = 't';
int index = -1;

for(int i=0;i<length;i++)
{
    if(str[i]== x)
    {
        index = i;
        break;
    }
}
if(index == -1)
{
    printf("%c Letter not found in string!",x);
}
else
{
    printf("%c character is at position %d",x,index);
}
}
